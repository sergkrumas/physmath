        # def easing():
          # target_value = 
          # current_value = 
          # FIXED = 10
          # delta = target_value - current_value
          # delta = round((FIXED*delta)/(FIXED+1))
          # return_value = target_value - delta
        # --------------------------------
          # cursor_pos = QCursor().pos()
          # topleft = QPoint(self.x(), self.y())
          # FIXED = 10
          # delta = cursor_pos - topleft
          # delta = QPoint(
          #     round(FIXED*delta.x()/(FIXED+1)),
          #     round(FIXED*delta.y()/(FIXED+1))
          #     )
          # self.move(cursor_pos - delta)